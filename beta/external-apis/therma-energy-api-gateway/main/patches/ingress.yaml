apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: therma-energy-api-gateway
spec:
  tls:
  - hosts:
      - energy-gateway.beta.helloatka.com
    secretName: energy-api-gateway-tls
  rules:
   - host: energy-gateway.beta.helloatka.com
     http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: therma-energy-api-gateway
              port:
                number: 80
