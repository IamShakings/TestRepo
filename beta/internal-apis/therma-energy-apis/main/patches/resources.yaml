apiVersion: apps/v1
kind: Deployment
metadata:
  name: therma-energy-apis
spec:
  template:
    spec:
      containers:
        - name: application
          resources:
            limits:
              cpu: 1000m
              memory: 1600Mi
            requests:
              cpu: 100m
              memory: 200Mi
