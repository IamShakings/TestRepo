apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: therma-location-api-service
spec:
  rules:
  - host: location-api.beta.hellotherma.it
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: therma-location-api-service
              port:
                number: 80