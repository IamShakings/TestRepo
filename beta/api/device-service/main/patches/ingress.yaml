apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: therma-device-service
spec:
  rules:
  - host: device-service.beta.hellotherma.it
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: therma-device-service
              port:
                number: 80