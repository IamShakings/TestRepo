apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: therma-alert-api-service
spec:
  rules:
  - host: alert-api.beta.hellotherma.it
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: therma-alert-api-service
              port:
                number: 80