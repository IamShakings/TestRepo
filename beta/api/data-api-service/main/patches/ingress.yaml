apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: therma-data-api-service
spec:
  rules:
  - host: data-api.beta.hellotherma.it
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: therma-data-api-service
              port:
                number: 80